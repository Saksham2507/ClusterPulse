route:
  receiver: clusterpulse-daemon
  group_by: ['alertname', 'instance']
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: clusterpulse-daemon
    webhook_configs:
      - url: http://remediation-daemon:8088/api/v1/alerts
        send_resolved: true
